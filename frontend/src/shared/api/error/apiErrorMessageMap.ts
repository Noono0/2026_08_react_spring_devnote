/**
 * ============================================================================
 * apiErrorMessageMap.ts — 서버 오류 코드를 사람이 읽을 문구로 바꾸는 사전
 * ============================================================================
 *
 * [왜 이런 사전이 필요한가?]
 *   서버가 보내는 오류 코드는 "DOCUMENT_VERSION_CONFLICT" 같은 개발자용 문자열이다.
 *   이걸 그대로 화면에 띄우면 사용자는 무슨 소린지 알 수 없다.
 *   그래서 코드 → 한국어 문장으로 번역해 주는 표를 따로 둔다.
 *
 * [서버가 보내는 message를 그냥 쓰면 안 되나?]
 *   그래도 되고 실제로 그게 기본 동작이다. (아래 3순위)
 *   다만 이 사전을 두면 이런 이점이 있다.
 *     - 서버를 안 고치고도 프론트에서 문구를 다듬을 수 있다
 *     - 나중에 다국어 지원을 붙일 때 여기만 바꾸면 된다
 *     - 서버가 기술적인 영어 메시지를 보내도 사용자에게는 친절한 한국어가 나간다
 *
 * [실제로 어떻게 쓰이나 — applicationNotification.apiError()]
 *   1순위: 이 사전에 등록된 문구
 *   2순위: 서버가 보낸 detail
 *   3순위: "요청을 처리하지 못했습니다." (최후의 기본값)
 *
 * [Record<string, string>이란?]
 *   "문자열 키에 문자열 값이 붙은 객체"라는 뜻의 타입이다.
 *   여기 없는 코드를 조회하면 undefined가 나오는데,
 *   쓰는 쪽에서 `??`로 기본값을 주고 있으므로 안전하다.
 *
 * ★ 새 오류 코드를 백엔드에 추가했다면, 여기에도 한 줄 추가해 주자.
 */
export const apiErrorMessageMap: Record<string, string> = {
  // ── 공통 오류 ─────────────────────────────────────────────
  COMMON_INVALID_REQUEST: "입력한 내용을 다시 확인해 주세요.",
  COMMON_INTERNAL_SERVER_ERROR: "서버에서 요청을 처리하지 못했습니다.",

  // ── 로그인·권한 ───────────────────────────────────────────
  AUTHENTICATION_REQUIRED: "로그인이 필요하거나 로그인 정보가 만료되었습니다.",
  ACCESS_DENIED: "이 기능을 사용할 권한이 없습니다.",
  LOGIN_FAILED: "아이디 또는 비밀번호가 올바르지 않습니다.",
  ACCOUNT_NOT_ACTIVE: "현재 사용할 수 없는 계정입니다.",
  LOGIN_ID_ALREADY_EXISTS: "이미 사용 중인 아이디입니다.",
  EMAIL_ALREADY_EXISTS: "이미 가입된 이메일입니다.",

  // ── 투표 기능 ─────────────────────────────────────────────
  POLL_NOT_FOUND: "투표를 찾을 수 없습니다.",
  POLL_CLOSED: "종료된 투표에는 참여할 수 없습니다.",
  POLL_OPTION_INVALID: "해당 투표의 선택지가 아닙니다.",
  POLL_ALREADY_VOTED: "이미 이 투표에 참여했습니다.",
  POLL_SELECTION_COUNT_INVALID: "선택할 수 있는 문항 수를 확인해 주세요.",
  POLL_END_TIME_INVALID: "투표 종료 시간은 현재부터 60분 이내로 설정해 주세요.",
  POLL_EDIT_NOT_ALLOWED: "참여자가 있는 투표의 문항은 변경할 수 없습니다.",
  POLL_STATUS_INVALID: "현재 투표 상태에서는 요청한 상태로 변경할 수 없습니다.",

  // ── 문서 기능 ─────────────────────────────────────────────
  DOCUMENT_NOT_FOUND: "삭제되었거나 존재하지 않는 문서입니다.",
  DOCUMENT_EDIT_FORBIDDEN: "이 문서를 수정할 권한이 없습니다.",
  // ★ 낙관적 잠금(optimistic locking) 오류.
  //   A와 B가 같은 문서를 동시에 열어 편집하다가 A가 먼저 저장하면,
  //   나중에 저장하는 B는 A의 수정 내용을 통째로 덮어써 버린다.
  //   그래서 서버가 버전 번호를 확인해 "이미 바뀐 문서다"라고 막아 준다.
  //   DocumentEditorPage에서 이 코드를 어떻게 처리하는지 확인해 보자.
  DOCUMENT_VERSION_CONFLICT: "다른 사용자가 먼저 수정했습니다. 최신 내용을 확인해 주세요.",

  // ── 파일 업로드 ───────────────────────────────────────────
  FILE_ACCESS_FORBIDDEN: "다른 사용자가 올린 파일은 현재 문서에 사용할 수 없습니다.",
  FILE_NOT_IMAGE: "대표 이미지에는 이미지 파일만 사용할 수 있습니다.",
  FILE_NOT_FOUND: "파일을 찾을 수 없습니다.",
  FILE_EXTENSION_NOT_ALLOWED: "업로드할 수 없는 파일 형식입니다.",
  FILE_SIZE_EXCEEDED: "업로드 가능한 파일 크기를 초과했습니다.",

  // ── 네트워크 ──────────────────────────────────────────────
  // 이건 서버가 보낸 코드가 아니라 apiErrorHelpers.ts가 직접 만들어 붙인 코드다.
  // 서버에 닿지도 못했으니 서버가 뭘 보낼 수가 없기 때문이다.
  NETWORK_CONNECTION_FAILED: "백엔드 서버에 연결할 수 없습니다.",
};
